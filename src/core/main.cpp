#include <iostream>
#include "lee_filter/lee_filter.hpp"
#include "MatrixUtils.h"

int main(int argc, char* argv[])
{
    // MatrixUtils::test_save_and_load();
    // auto _ret = test_lee_filter();
    // return (_ret.match() || _ret.max_diff() <= CONST_ACCEPTABLE_MAX_DIFF_ERROR ? EXIT_SUCCESS : EXIT_FAILURE);
    // MatrixUtils::test_save_and_load();
    return EXIT_SUCCESS;
}